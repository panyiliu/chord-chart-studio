VITE_BASE=/app

# --- 本地 AI（仅开发：复制为 .env.local 后 yarn dev；会注入空引擎列表时的默认一条）---
# 密钥会进入前端 bundle，勿提交仓库。
# VITE_AI_DEFAULT_NAME=Local
# VITE_AI_DEFAULT_BASE_URL=https://api.openai.com/v1
# VITE_AI_DEFAULT_MODEL=gpt-4o-mini
# VITE_AI_DEFAULT_API_KEY=
